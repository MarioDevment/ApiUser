monolog:
    handlers:
        main:
            type: stream
            path: "%kernel.logs_dir%/%kernel.environment%/%kernel.environment%.test.log"
            level: debug
            channels: ["!event"]
